# 在本地跑大模型 · 问题梳理与答疑

> 主线：在本地把一个大模型跑起来，并跑得快。
> 两条路：① **API key 捷径**——直接调云端 messages 接口，跳过本地的一切；② **本地路线**——自己加载模型运行，按决策顺序分四阶段。
> 示例环境：Apple M4 Pro / 64GB / macOS，本地装有 Ollama。下文的实测数字均基于该配置，换机器需自行调整。

---

## 逻辑骨架

```
目标：在本地跑起一个可用的模型——有两条路：

路线 A · API key 捷径 ── 不本地跑，直接调云端 messages 接口
        跳过下面整条本地链路；代价：看不到模型加载 / 格式 / 硬件这些底层（格式见 02 号文档）

路线 B · 本地跑模型 ── 走完整链路，按决策顺序分四阶段：
  │
  ├─ 一、选型 ── 能跑什么？             → Q1 判断方法 · Q2 套到具体模型
  ├─ 二、获取 ── 模型从哪来？           → Q3 下载怎么办 · Q4 复用 Ollama
  ├─ 三、运行 ── 用什么在跑、谁在加速？  → Q5 库vs服务 · Q6 加速是谁做的
  └─ 四、性能 ── 跑多快、有什么格式坑？  → Q7 Mac优化 · Q8 Mac vs Win · Q9 nvfp4
```

> 下表为**路线 B（本地）**的四阶段明细（路线 A 见 02 号文档）：

| 阶段 | 核心决策 | 涉及问题 |
|---|---|---|
| **一、选型** | 这台机器能跑什么模型？ | Q1、Q2 |
| **二、获取** | 模型文件从哪来、怎么来？ | Q3、Q4 |
| **三、运行** | 用什么引擎在跑？加速谁做的？ | Q5、Q6 |
| **四、性能与格式** | 能跑多快？哪些格式有坑？ | Q7、Q8、Q9（对话格式见 02 号文档） |

---

## 一、选型：能跑什么？

### Q1 · 怎么判断一个模型本机能不能跑？（通用方法）
- **结论**：两条公式。一、**装得下** = GGUF 文件大小 + 约 2GB < 可用内存（macOS 默认把约 87% 内存分给 GPU 当显存，以 64GB 为例约 55.6GB）；二、**跑得动** = 内存带宽 ÷ 文件大小 ≈ token/s 上限（M4 Pro 内存带宽约 273 GB/s）。以 M4 Pro / 64GB 为例：甜点区 ≤32B，上限约 70B-Q4。
- **文档**：[GGUF 格式说明](https://github.com/ggml-org/ggml/blob/master/docs/gguf.md) · [Apple Silicon 跑 LLM 实测/选购](https://localaimaster.com/blog/apple-silicon-ai-buying-guide)

### Q2 · 一个 8B 模型能跑吗？（套用 Q1）
- **结论**：能，余量充足——8B-Q4 只需约 6–7GB 内存。但 `llama-cpp-python` 的 `Llama()` 默认 `n_gpu_layers=0`（纯 CPU）较慢；设 `n_gpu_layers=-1` 把全部层放到 Metal GPU。以 M4 Pro 为例实测：7B-Q4 在 Metal 上约 30 tok/s（短样本，27~36 波动），纯 CPU 约 24 tok/s，约 1.5x——Apple Silicon 的 CPU 推理本身也不慢，Metal 是提升而非数量级差异。
- **文档**：[llama-cpp-python](https://github.com/abetlen/llama-cpp-python) · [llama.cpp Metal](https://github.com/ggml-org/llama.cpp)

---

## 二、获取：模型从哪来？

### Q3 · 直接从 HuggingFace 下载模型很慢，怎么办？
- **结论**：在走代理的网络下，从 HuggingFace 下几个 GB 往往又慢又不稳。常见根因：代理（如 ClashX 的 TUN 模式）在系统路由层劫持了全部流量，连国内镜像也被导向慢节点、无法直连，`--noproxy` 也绕不开。**正解：不下载，复用本地 Ollama 已有的 GGUF**（见 Q4）；或在代理侧放行镜像域名直连。
- **文档**：[Ollama 模型导入/存储](https://docs.ollama.com/import)

### Q4 · Ollama 已下载的模型能直接复用吗？
- **结论**：**能**。Ollama 把模型存成内容寻址 blob（文件名是 sha256、无 `.gguf` 后缀，看着不像模型），一个模型拆成 权重 + 模板 + params + license 多个 blob；权重 blob 的前 4 字节就是 `GGUF`，本身即标准 GGUF 文件。把权重层软链或复制成 `.gguf`，即可被 llama-cpp-python 直接加载，无需重新下载。**例外**：以 nvfp4 等量化分发的模型是 safetensors 而非 GGUF（见 Q9），不适用。
- **文档**：`~/.ollama/models/{manifests,blobs}` · [Ollama import/storage](https://docs.ollama.com/import)

---

## 三、运行：用什么在跑？谁在加速？

### Q5 · llama-cpp-python 和 Ollama 有何区别？
- **结论**：llama-cpp-python = `from llama_cpp import Llama` 的**进程内库**，输入文本、输出文本，对话格式自己拼；Ollama = 后台**服务**（监听 `:11434`），HTTP 调用、自动套对话模板、自动用 GPU、模型常驻。两者底层引擎同为 llama.cpp。**库 vs 服务**是两者最本质的区别。
- **文档**：[Ollama API](https://docs.ollama.com/api) · [llama-cpp-python](https://github.com/abetlen/llama-cpp-python)

### Q6 · Mac 上的本地加速是谁提供的？是 Ollama 吗？
- **结论**：不是 Ollama 提供的。加速来自三层：一、**统一内存 UMA**（CPU/GPU 共享同一块内存、不搬运权重，最关键）；二、**Apple Metal**（GPU API）；三、推理引擎（llama.cpp 的 Metal 后端，或 MLX）。Ollama 只是调用这些；直接用 llama-cpp-python 同样能用满 Metal（设 `n_gpu_layers=-1`）。
- **文档**：[Ollama 引擎 PR #16031](https://github.com/ollama/ollama/pull/16031)（GGUF→llama.cpp，safetensors→MLX） · [Ollama × MLX 博客](https://ollama.com/blog/mlx)

---

## 四、性能与格式：跑多快？有什么坑？

### Q7 · 哪些模型是「为 Mac 优化」的？优化指什么？
- **结论**：GGUF 是**跨平台同一份文件**，不分 Mac / Win；Mac 上的加速来自**引擎层**（llama.cpp 的 Metal）。真正为 Apple Silicon 原生设计的模型格式是 **MLX**（HuggingFace 上 `mlx-community/` 大量发布），在 M 系芯片上 prefill 更快、内存更省。
- **文档**：[Apple MLX](https://github.com/ml-explore/mlx) · [mlx-community 模型库](https://huggingface.co/mlx-community) · [Apple Silicon 推理对比研究](https://arxiv.org/pdf/2511.05502)

### Q8 · 同样本地跑，Mac 比 Windows 慢吗？
- **结论**：与 **Windows + NVIDIA 独显**比，Mac 更慢（CUDA 成熟、显存带宽约 1000 GB/s，远高于 M4 Pro 的 273）；与 **Windows 无独显 / 核显**比，Mac 快很多；**跑超大模型**时，Mac 的 64GB 统一内存能装下 70B，而消费级单卡显存（如 24GB）装不下会 OOM。比速度，独显占优；比"能装多大模型 + 能效 + 免配置"，Apple Silicon 占优。跨平台的具体数字为数量级参考，非精确值。
- **文档**：[Apple Silicon 推理对比研究](https://arxiv.org/pdf/2511.05502) · [Mac 本地 AI 实测](https://localaimaster.com/blog/apple-silicon-ai-buying-guide)

### Q9 · nvfp4 是为 Mac 做的加速格式吗？为什么 llama.cpp 用不了？（联网核实）
- **结论**：nvfp4 是 **NVIDIA 的量化格式**，不是为 Mac 定制；它在 Mac 上可经 Ollama 的 MLX 引擎运行，但**不是 GGUF，llama-cpp-python 加载不了**。
  - nvfp4 = NVIDIA 为 Blackwell GPU 设计的 4-bit 浮点格式（E2M1），在 N 卡 Tensor Core 上有硬件加速。
  - Apple Silicon 没有 FP4 硬件；Ollama 在 Mac 上用 MLX 引擎运行 nvfp4，官方实测 prefill 1154→1810 tok/s（M5，+57%）确有加速。但该数据来自 M5；M4 Pro 缺 M5+ 的张量硬件（`ggml_metal` 报 `has tensor = false`），吃不到这部分加速。
  - 对 llama-cpp-python 无解：这类模型是 **safetensors + MLX**（按 tensor 分片、`file_type=nvfp4`、文件头非 GGUF），llama-cpp-python 只加载 GGUF。要用 nvfp4 加速需走 `ollama run` 或 mlx-lm。
- **文档**：[NVFP4 介绍 · NVIDIA](https://developer.nvidia.com/blog/introducing-nvfp4-for-efficient-and-accurate-low-precision-inference/) · [Ollama × MLX 博客](https://ollama.com/blog/mlx) · [引擎路由 PR #16031](https://github.com/ollama/ollama/pull/16031)

### 对话格式 / 结束符 → 见 `02-从LocalLLM理解对话格式与结束符.md`

模型的对话格式与结束符（eos）、为什么默认 stop 会与某些模型不匹配、以及对话格式适配在哪一层处理，整合在 **02 号文档**。

---

## 说明与适用范围

- **示例数据来源**：文中的速度、显存等实测数字基于 Apple M4 Pro / 64GB / macOS / llama-cpp-python / qwen2.5-7B（约 Q4）。换机器、换模型规模需自行调整。
- **外推项**：14B / 32B / 70B 的"能跑 / 速度"为按公式外推，未实跑；速度数字基于短样本，长上下文 / 长输出 / 并发场景未测。
- **跨平台项**：Mac 与 Windows / 独显的对比为数量级参考，具体数字勿当精确值。
- **前提条件**：
  - 走 Metal 需在 Apple Silicon 上使用带 Metal 的 llama-cpp-python；非 Apple Silicon 或 CPU-only 构建则无 Metal。
  - 复用 Ollama 模型需是单个 GGUF（manifest 含 `image.model` 层）；nvfp4 / safetensors 分片模型不适用。
  - 显存上限（约 87%）是 macOS 的动态值，随系统版本与内存压力变化。

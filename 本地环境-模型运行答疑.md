# 本地环境跑模型 · 问题梳理与答疑

> **一条主线**：在我的 Mac 上，给这个项目搞一个**能跑、且跑得快**的模型。
> 围绕它先有**两条路**：嫌麻烦可走「API key 捷径」（直接调云端，跳过本地一切）；走「本地路线」（原项目）则按决策顺序分四阶段。两条路 + 四阶段见下方逻辑骨架。
>
> 背景：Apple **M4 Pro / 64GB / macOS**；装了 **ClashX**（TUN 模式）；本地有 **Ollama**。起点 = `QUICKSTART.md` 第 2 步「下载 GGUF 模型」。

---

## 逻辑骨架

```
目标：在我的 Mac 上，给这个 agent 项目搞一个能用的模型——其实有两条路：

路线 A · API key 捷径 ── 不本地跑，直接调云端 messages 接口
        跳过下面整条本地链路；代价：看不到模型加载 / 格式 / 硬件这些底层（见 Q11）

路线 B · 本地跑模型（原项目）── 走完整链路，按决策顺序分四阶段：
  │
  ├─ 一、选型 ── 我能跑什么？           → Q1 判断方法 · Q2 套到具体模型
  ├─ 二、获取 ── 模型从哪来？           → Q3 第2步怎么办 · Q4 复用 Ollama
  ├─ 三、运行 ── 用什么在跑、谁在加速？  → Q5 库vs服务 · Q6 加速是谁做的
  └─ 四、性能 ── 跑多快、有什么格式坑？  → Q7 Mac优化 · Q8 Mac vs Win · Q9 nvfp4 · Q10 模板适配 · Q11 适配在哪层
```

> 下表为**路线 B（本地）**的四阶段明细（路线 A 见 Q11）：

| 阶段 | 核心决策 | 涉及问题 |
|---|---|---|
| **一、选型** | 我这台机器能跑什么模型？ | Q1、Q2 |
| **二、获取** | 模型文件从哪来、怎么来？ | Q3、Q4 |
| **三、运行** | 用什么引擎在跑？加速谁做的？ | Q5、Q6 |
| **四、性能与格式** | 能跑多快？哪些格式有坑？ | Q7、Q8、Q9、Q10、Q11 |

---

## 一、选型：我能跑什么？

### Q1 · 怎么判断一个模型本机能不能跑？（通用方法）
- **结论**：两条公式。一 **装得下** = GGUF 文件大小 + ~2GB < 可用内存（注意 macOS 默认给 GPU 约 87% 内存当显存——本机实测 recommendedMaxWorkingSetSize ≈ 55.6GB）；二 **跑得动** = 内存带宽（M4 Pro ≈ 273 GB/s）÷ 文件大小 ≈ token/s 上限。本机甜点区 ≤32B，上限 70B-Q4。
- **文档**：[GGUF 格式说明](https://github.com/ggml-org/ggml/blob/master/docs/gguf.md) · [Apple Silicon 跑 LLM 实测/选购](https://localaimaster.com/blog/apple-silicon-ai-buying-guide)

### Q2 · 具体到这个 Llama-3-8B，能跑吗？（套用 Q1）
- **结论**：能，绰绰有余——只需 ~6–7GB 内存，本机 64GB。但 `shared/llm.py` 的 `Llama()` 默认 `n_gpu_layers=0`（纯 CPU）所以慢；加 `n_gpu_layers=-1` 把全部层丢给 Metal GPU。**本机已端到端实测**：29/29 层上 Metal、qwen2.5-7B 实测 ~30 tok/s（短样本波动 27~36）；对比纯 CPU ~24 tok/s ≈ 1.5x——注意 M4 Pro 的 CPU 也不慢，Metal 属锦上添花而非天壤之别。
- **文档**：[`shared/llm.py:41`](./shared/llm.py) · [llama-cpp-python](https://github.com/abetlen/llama-cpp-python) · [llama.cpp Metal](https://github.com/ggml-org/llama.cpp)

---

## 二、获取：模型从哪来？

### Q3 · QUICKSTART 第 2 步「下载模型」该如何处理？（最初的问题）
- **结论**：照文档去 HuggingFace 下 5GB 在此网络环境下又慢又折腾——根因是 **ClashX TUN 模式**在系统路由层劫持了全部流量（连国内镜像也被塞进慢节点，`--noproxy` 绕不开），实测任何源都卡 ~500K、要 3 小时。**正解：不下载，复用本地 Ollama 已有的 GGUF**（见 Q4）。
- **文档**：[`QUICKSTART.md`](./QUICKSTART.md) 第 2 步 · [Ollama 模型导入/存储](https://docs.ollama.com/import)

### Q4 · Ollama 已有的模型能直接复用 / 裸跑吗？
- **结论**：**能**。Ollama 把 GGUF 存成内容寻址 blob（文件名是 sha256、无 `.gguf` 后缀，所以“看着不像模型”），一个模型拆成 权重+模板+params+license 多个 blob；实测权重 blob 前 4 字节就是 `GGUF`。把权重层软链/复制成 `.gguf` 即可被 llama-cpp-python 裸跑。已把 `qwen2.5-7b` → `models/llama-3-8b-instruct.gguf`（项目 11 处路径零改）。**例外**：nvfp4 那几个是 safetensors 不是 GGUF（见 Q9）。
- **文档**：`~/.ollama/models/{manifests,blobs}` · [Ollama import/storage](https://docs.ollama.com/import)

---

## 三、运行：用什么在跑？谁在加速？

### Q5 · 项目的加载方式（llama-cpp-python）和我本地 Ollama 有何区别？
- **结论**：本项目 = `from llama_cpp import Llama` **进程内库**，文本进文本出、自己拼 prompt；Ollama = 后台**服务**（`:11434`），HTTP 调用、自动 chat 模板、自动 GPU、模型常驻。底层引擎同为 llama.cpp。库 vs 服务，是这两者最本质的差别。
- **文档**：[`shared/llm.py`](./shared/llm.py) · [Ollama API](https://docs.ollama.com/api) · [llama-cpp-python](https://github.com/abetlen/llama-cpp-python)

### Q6 · Mac 上的本地加速，到底是谁做的？是 Ollama 吗？
- **结论**：**不是 Ollama 的功劳**。三层：一 **统一内存 UMA**（CPU/GPU 共享 64GB、不搬运权重，最关键）；二 **Apple Metal**（GPU API）；三 引擎（**llama.cpp Metal 后端** 或 **MLX**）。Ollama 只是调用它们；本项目用 llama-cpp-python 裸跑同样吃满 Metal（只要开 `n_gpu_layers=-1`）。
- **文档**：[Ollama 引擎 PR #16031](https://github.com/ollama/ollama/pull/16031)（GGUF→llama.cpp，safetensors→MLX） · [Ollama × MLX 博客](https://ollama.com/blog/mlx)

---

## 四、性能与格式：跑多快？有什么坑？

### Q7 · 哪些模型是「为 Mac 优化」的？优化指什么？
- **结论**：GGUF 是**跨平台同一份文件**，不分 Mac/Win 版；Mac 上的加速来自**引擎层**（llama.cpp 的 Metal）。真正“为 Apple Silicon 原生设计”的模型格式是 **MLX**（HuggingFace `mlx-community/` 大量发布），M 芯上 prefill 更快、内存更省。
- **文档**：[Apple MLX](https://github.com/ml-explore/mlx) · [mlx-community 模型库](https://huggingface.co/mlx-community) · [Apple Silicon 推理对比研究](https://arxiv.org/pdf/2511.05502)

### Q8 · 同样裸跑，Mac 比 Windows 慢吗？
- **结论**：**vs Win + NVIDIA 独显**——Mac 慢（CUDA + ~1000 GB/s 显存碾压 M4 Pro 的 273）；**vs Win 无独显/核显**——Mac 快很多；**跑超大模型**——Mac 64GB 统一内存能装 70B，消费级单卡显存（24GB）装不下反而 OOM。拼速度 N 卡赢，拼“能装多大 + 能效 + 免配置”Mac 赢。
- **文档**：[Apple Silicon 推理对比研究](https://arxiv.org/pdf/2511.05502) · [Mac 本地 AI 实测](https://localaimaster.com/blog/apple-silicon-ai-buying-guide)

### Q9 · nvfp4 是 Ollama 为 Mac 做的加速模型吗？为何被排除？（联网核实）
- **结论**：nvfp4 是 **NVIDIA 的量化格式**，不是“为 Mac 定制的加速模型”；它在 Mac 上能经 Ollama-MLX 加速，但**非 GGUF、无法用于本项目**。
  - nvfp4 = **NVIDIA** 为 Blackwell GPU 设计的 4-bit 浮点格式（E2M1），N 卡上 Tensor Core 硬件原生加速。
  - Apple Silicon **没有** FP4 硬件；但 **Ollama 在 Mac 上用 Apple 的 MLX 引擎**跑 nvfp4，官方实测 **prefill 1154→1810 tok/s（M5，+57%）确有加速**。本机 M4 Pro 实测也印证——`ggml_metal` 报 `has tensor = false`、`tensor API disabled for pre-M5`，即缺 M5+ 的张量硬件，所以那 +57% 是 M5 数据、M4 Pro 吃不到。
  - **但对本项目无解**：这些模型是 **safetensors + MLX**（实测 760 个 tensor 分片、`file_type=nvfp4`、magic 非 GGUF），本项目的 **llama-cpp-python 只吃 GGUF、加载不了**。想用 nvfp4 加速请走 `ollama run` 或 mlx-lm，不能裸跑进本项目。
- **文档**：[NVFP4 介绍 · NVIDIA](https://developer.nvidia.com/blog/introducing-nvfp4-for-efficient-and-accurate-low-precision-inference/) · [Ollama × MLX 博客](https://ollama.com/blog/mlx) · [引擎路由 PR #16031](https://github.com/ollama/ollama/pull/16031)

### Q10 · 模型真身是 qwen2.5，项目却用老式 Vicuna 风格模板：区别、影响、改法、对学习的意义

**两模型的区别**——不在能力，而在**对话格式约定（chat template）**：instruct 模型训练时各用一套特殊 token 包装对话，连结束符都不同。

| | qwen2.5（模型真身） | 项目代码假设的格式 |
|---|---|---|
| 轮次标记 | `im_start` / `im_end` | `User:` / `Assistant:` |
| 结束符 eos | im_end（id=151645） | </s>（对 qwen 无效） |
| 流派 | ChatML（GGUF 实测自带） | 老式 Vicuna 风格（2023 时代） |

- **有什么影响**：项目把老式 Vicuna 风格裸文本 + stop 喂给 qwen——`</s>` 对 qwen **完全无效**（非它的 token），这条 stop 形同虚设；`\n\n` **会误伤**（回答含空行即被提前截断）；真正让 qwen 停的 `<|im_end|>`（eos）项目没显式用，靠 llama.cpp 底层 eos 兜底；输入端**没 ChatML 包装** → system 指令 / 多轮边界识别不准，**指令遵循、多轮一致性、JSON 稳定性打折**。实测单轮短问答**能出通顺中文**，但越复杂（多轮、结构化、tool calling）越易暴露。
- **哪段代码有问题**：`shared/llm.py:68` 的 stop `["</s>", "\n\n", "User:", "Assistant:"]` 是老式 Vicuna 风格（`USER:/ASSISTANT:` + `</s>`）、对 qwen 错配；`shared/llm.py:74` 用 `self.llm(**kwargs)`（raw completion）而非 `create_chat_completion`（后者才套内置 ChatML）；`agent.py:96` 的 `generate_with_role` 手拼 `User:/Assistant:` 裸格式。根源：整套设计**假设模型用老式 `USER:/ASSISTANT:` 格式**，换 qwen（ChatML）后假设不成立。
- **如何改、为什么**：**最小改**——stop 换成 `["<|im_end|>"]`，消除 `</s>` 无效、`\n\n` 误伤；**最佳改**——`llm.py:74` 换 `self.llm.create_chat_completion(messages=[...])`，让 llama-cpp-python **自动读 GGUF 内置 chat_template** + 用正确 eos，彻底解耦“模型家族”与代码、换任何模型自动适配。**取舍**：`create_chat_completion` 把“对话格式”这层魔法又封回去，与项目“看清裸机制”的教学初衷有张力——所以教学版保留 raw 是**有意为之**。
- **对学习的影响**：**正面**——raw + 手拼 prompt 让人看清“chat 模型本质是文本续写、对话格式是人为约定”，这是被 Ollama / `create_chat_completion` 藏起来的底层认知；**陷阱**——若不知模型真身是 qwen，易误以为 `</s>` 在起作用、把输出被截断或 JSON 不稳**归错因**（怪模型笨，实为格式没喂对）、把“老式 `USER:/ASSISTANT:` 格式”当通用真理；**结论**——教学用 raw 没错，但须知在用非标准格式，到第 03 课（结构化输出）、05 课（tool calling）等格式敏感处应换 ChatML / `create_chat_completion` 对照。
- **文档**：`shared/llm.py:68`（stop）· `shared/llm.py:74`（raw 调用）· `agent.py:96`（拼 prompt）· 实测 `test_agent_chain.py`

### Q11 · 对话格式的适配在哪一层做？直接用 API key 呢？

**“统一”不是把格式变一样**（格式仍各异），而是用两层抽象把差异吸收掉：

```
④ 应用调用    messages=[{role, content}]                      ← 只写这一层，统一
③ 库高层 API  apply_chat_template / create_chat_completion    ← 读 ② 自动套对格式
② chat_template 字段（Jinja2 模板，随模型分发）                 ← ★真正的“统一承载层”★
   存于 tokenizer_config.json / GGUF metadata                  格式各异，但“存法”统一
① tokenizer   特殊 token（<|im_start|> / <|eot_id|>）          ← 物理基础，各家不同
```

- **第 ② 层是关键**：HuggingFace 立的规矩——把“如何把对话渲染成该模型要的字符串”写成一段 Jinja2 模板（`chat_template`），**随模型文件一起分发**（Q10 实测 qwen 的 GGUF 里那段就是它）。格式内容仍 ChatML / Llama-3 / Vicuna 各不同，但**存放方式被统一了**。
- **第 ③ 层是统一接口**：`apply_chat_template()`（transformers）、`create_chat_completion()`（llama-cpp-python）等——读 ② 的模板、自动套对。
- 所以**被统一的是“格式的存法 + 调用接口”，不是格式本身**。

**直接用 API key（云端 API）**：①②③ 全在服务端，调用方发出去的就是 `messages=[{role, content}]`，服务端负责套模板、加特殊 token、推理——**格式问题对调用方完全透明、彻底消失**。这正是 OpenAI / Anthropic 的 `messages` 接口成为行业通用语的原因。

| 调用方式 | 格式 / 模板由谁套 | 要懂格式吗 |
|---|---|---|
| 云 API（API key） | 服务端 | 否，只传 messages |
| 本地高层 API（`create_chat_completion` / Ollama / `apply_chat_template`） | 库自动读内置 `chat_template` | 否，只传 messages |
| 本地 raw（本项目 `self.llm(prompt)`） | **调用方手拼 prompt + stop** | **是**，所以会错配 → Q10 |

- **一句话**：Q10 那个坑，根源就是本项目选了最底层的 raw（绕过 ②③ 自己拼）；往上一层（`create_chat_completion`）或用 API key，模板差异就自动被吃掉。本项目故意待在最底层，是为了让人**看见**这层平时被吃掉的东西。
- **文档**：延伸自 Q5（库 vs 服务）、Q10（模板差异）· HuggingFace `chat_template` 标准 · `apply_chat_template` / `create_chat_completion`

---

## 当前进度 / 下一步

- ✅ **二、获取** 完成：`models/llama-3-8b-instruct.gguf` 已就位（qwen2.5-7b，~Q4，4.4G，magic=GGUF）
- ✅ **装库** 完成：`venv` + `llama-cpp-python 0.3.24`（Python 3.14 本地编译，自带 Metal）
- ✅ **三、运行 端到端实测**（`test_metal.py`）：`n_gpu_layers=-1` → **29/29 层上 Metal GPU**（M4 Pro，显存上限实测 ≈55.6GB）；qwen2.5-7B **实测 ~30 tok/s**（对比纯 CPU ~24，约 1.5x）；输出中文通顺 →「能裸跑通」已证实
- ✅ **真实调用链已验**（`test_agent_chain.py`）：`Agent → LocalLLM.generate()` 跑通——`simple_generate` / `generate_with_role` 均返回通顺中文、默认 stop 行为正常（Metal 为“默认 -1 + 速度”推断，日志被 `disable_llama_logging` 抑制、未直接抓到）；仅覆盖第 01/02 课方法，tool / planner / memory 等后续课与完整 `complete_example.py` 未跑
- ⏳ 可选：跑完整 `python complete_example.py`（覆盖全部 10 课）
- 备注：模型实际是 qwen2.5（ChatML 模板），项目 stop 序列是老式 Vicuna 风格（`shared/llm.py:68`），raw completion 实测能出通顺中文；想更干净可改成 `<|im_end|>`

---

## 适用边界与局限（本文档结论的有效范围）

> 一句话：核心“能跑”类结论已在**这一台机器、这一个 7B 模型**上实测；其余为规格值、外推或跨平台引用，换条件可能不成立。

**一、实测覆盖的边界**
- 实测环境仅为：Mac mini M4 Pro / 64GB / macOS / `llama-cpp-python 0.3.24` / Python 3.14 / qwen2.5-7B（约 Q4）这一组合。
- 模型规模：**只实跑了 7B**；对 14B / 32B / 70B 的“能跑 / 速度”均为**按公式外推、未实跑**。
- 速度数字：基于 **20~25 token 的短生成**，样本小、波动大（Metal 实测 27~36 tok/s）；长上下文、长输出、并发场景未测。

**二、明确未验证的条目**
- Q5 Ollama 的 `:11434` / 自动模板 / 模型常驻：凭领域知识，**未调用本机 Ollama 实测**。
- Q1 内存带宽 273 GB/s：M4 Pro 规格值，未本机 benchmark。
- Q7 MLX 比 GGUF 快：联网结论，未本机对比。
- Q8 Mac vs Windows 的具体数字：跨平台、无 Windows 机器，**无法验证**——数量级方向可信，**具体数字勿当精确值**。
- Q9 的 1154→1810 tok/s：Ollama 官方 **M5** 数据，**不代表本机 M4 Pro**。

**三、前提条件（不满足则结论失效）**

- 走 Metal 的前提：`llama-cpp-python` 在 Apple Silicon 上自带 Metal（本机为 Python 3.14 源码编译、默认开启）；非 Apple Silicon 或 CPU-only wheel 则无 Metal。
- 复用 Ollama 模型的前提：必须是**单 GGUF**（manifest 含 `image.model` 层）；**nvfp4 / safetensors 分片模型不适用**（见 Q9）。
- 下载慢的结论：基于 **ClashX TUN 全局劫持**的当前网络；换网络或关闭 TUN 后，该结论不再成立。

**四、已知的坑 / 例外**
- 模型真身是 **qwen2.5（ChatML）**，项目 stop 序列却是老式 Vicuna 风格（`USER:/ASSISTANT:` + `</s>`）：raw completion 能出通顺中文，但**不是该模型的标准对话格式**；严肃使用建议改用 ChatML 模板与 stop（`<|im_end|>`）。
- 显存上限 ~87%（55.6GB）是 macOS 默认**动态值**，随系统版本 / 内存压力变化，非固定。
- M4 Pro 上**纯 CPU 也不慢**：实测 ~24 tok/s，Metal 仅约 1.5x 提速；该结论与机器强相关，换低端机器会不同。

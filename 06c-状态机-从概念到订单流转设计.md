# 状态机：从概念到订单流转设计

> 本文从 lesson6 的 `run_loop` 引出"有限状态机（FSM）"这一通用模型，分两部分：
> 一、FSM 是什么、怎么理解、在哪应用（§1–§3）
> 二、把 FSM 落到一个真实业务：电商订单状态机的完整设计方案（§4）
> 结论均附信源（教材 / 官方文档 / RFC），见 §6。
>
> 怎么读：想要概念读 §1–§3；想直接看工程方案翻 §4；回连 lesson6 看 §5。
> 配套：lesson6 本身的 agent 循环与 `AgentState` 状态管理，见 `06-lesson6答疑-agent循环与状态管理.md`。

---

## 1 状态机（FSM）是什么

状态机（**Finite State Machine, FSM**，有限状态机）是 CS"自动机理论 / 计算理论"里最基础的模型之一，属教科书级共识。

**经典五元组定义** `(Q, Σ, δ, q₀, F)`：

1. **有限个状态** Q（可枚举、有名字，如 `Locked` / `Unlocked`）
2. **一组输入/事件** Σ（如"投币""推杆"）
3. **转移函数** δ: `状态 × 事件 → 新状态`（核心）
4. **初始状态** q₀
5. **终止/接受状态** F（可选）

**三条铁律**：任意时刻**恰好处于一个**状态；状态切换**只能由事件触发**且**必须遵循预定义转移规则**；状态数**有限可枚举**。

**起源（History）**：

| 时间 | 人物 | 贡献 |
|---|---|---|
| 1943 | McCulloch & Pitts | *A Logical Calculus Immanent in Nervous Activity*，**首次描述有限自动机** |
| 1955 | G.H. Mealy | Mealy 机（输出取决于状态+输入） |
| 1956 | E.F. Moore | *Gedanken-Experiments on Sequential Machines*，Moore 机（输出仅取决于状态） |
| 1956 | Kleene | 正则表达式 ⟺ 有限自动机等价（Kleene 定理） |

标准教材：**Sipser《Introduction to the Theory of Computation》第 1 章**（finite automaton 五元组定义）、**Hopcroft & Ullman《Introduction to Automata Theory, Languages, and Computation》**。

---

## 2 怎么理解（闸机最小例子）

FSM 把"系统行为"收敛成一张表：**当前处境 + 来了什么事件 → 该去哪个处境**。最小真实例子——地铁闸机（Wikipedia FSM 条目的标准教学例子）：

| 当前状态 | 事件 | → 新状态 |
|---|---|---|
| Locked | 投币 | Unlocked |
| Unlocked | 推杆通过 | Locked |
| Locked | 推杆（没投币） | Locked（无效） |
| Unlocked | 再投币 | Unlocked（浪费） |

**它解决什么问题**：把散落的 `if/else` 收敛成一张明确的转移表，并**天然禁止非法跳转**——这是 FSM 最大的工程价值：**让"不该发生的状态切换"在建模层就无法表达**。

---

## 3 在哪里应用

| 领域 | 例子 |
|---|---|
| 网络协议 | TCP 连接：`CLOSED→LISTEN→SYN_SENT→ESTABLISHED→…→CLOSED`（RFC 9293 含状态图） |
| 文本解析 | 正则引擎（DFA/NFA）、词法分析器（lexer）、JSON/协议 parser |
| 业务流程 | 订单：`待付款→已付款→已发货→已签收 / 已退款`（见 §4） |
| UI / 游戏 | 表单向导、角色状态 `idle/run/jump`、加载态 `loading/success/error` |
| 硬件 / 嵌入式 | 红绿灯、电梯、自动售货机 |

---

## 4 业务落地：电商订单状态机设计方案

### 4.1 为什么订单要用状态机

订单生命周期是 FSM 的教科书级场景：状态有限可枚举、转移严格受约束、**非法转移必须在建模层堵死**（"已退款"不能再"发货"）。不用状态机就退化成散落各处的 `if (status==...)`，随状态增多而爆炸，且漏掉非法转移 = 资损 bug。业界共识：**状态模式 / 状态机是订单流转的标准解法**。状态机三要素：**状态 + 事件 + 动作**，事件驱动转移并触发动作，转移可带**守卫（guard）** 条件。

### 4.2 状态定义（States）

| 状态 | 含义 | 类型 |
|---|---|---|
| `CREATED` | 已创建 / 待付款 | 初始态 |
| `PAID` | 已付款 / 待发货 | 中间态 |
| `SHIPPED` | 已发货 / 配送中 | 中间态 |
| `COMPLETED` | 已签收 / 已完成 | **终态** |
| `CANCELLED` | 已取消（超时/主动） | **终态** |
| `REFUNDING` | 退款中（逆向流程） | 中间态 |
| `REFUNDED` | 已退款 | **终态** |

> 有的设计把"已签收 `RECEIVED`"与"已完成 `COMPLETED`"（售后期结束）拆开；本方案合并。终态 = FSM 的"接受状态"，订单到此生命周期结束。

### 4.3 事件（Events）

`submit`（下单）· `pay`（支付成功回调）· `payTimeout`（支付超时）· `cancel`（用户/系统取消）· `ship`（商家发货）· `confirm`（确认收货：用户主动 or 超时自动）· `applyRefund`（申请退款）· `refundDone`（退款到账）

### 4.4 转移表 δ：`(当前状态 × 事件) → 新状态`（核心）

这张表是整个设计的"真相源"，带 guard（守卫）和 action（副作用）：

| 当前状态 | 事件 | guard 守卫 | → 新状态 | action 动作 |
|---|---|---|---|---|
| `CREATED` | `pay` | 金额校验通过 | `PAID` | 真实扣库存、通知商家 |
| `CREATED` | `payTimeout`/`cancel` | — | `CANCELLED` | 释放库存预占 |
| `PAID` | `ship` | 有货 | `SHIPPED` | 生成物流单 |
| `PAID` | `applyRefund`/`cancel` | 未发货 | `REFUNDING` | 冻结订单、发起退款 |
| `SHIPPED` | `confirm` | — | `COMPLETED` | 结算、放款给商家 |
| `SHIPPED` | `applyRefund` | 售后期内 | `REFUNDING` | 退货退款流程 |
| `COMPLETED` | `applyRefund` | 售后期内 | `REFUNDING` | 售后退款 |
| `REFUNDING` | `refundDone` | — | `REFUNDED` | 原路退款、恢复库存 |

**非法转移（δ 未定义）一律拒绝/抛异常**——`CANCELLED + ship`、`REFUNDED + pay`、`COMPLETED + ship`… 这正是状态机最大价值。

### 4.5 状态图（state diagram）

```
          submit          pay              ship            confirm
  [*] ───────────> CREATED ─────> PAID ─────────> SHIPPED ─────────> COMPLETED ●
                   待付款 │      待发货 │          配送中 │              │
        payTimeout/cancel │  cancel/    │ applyRefund     │ applyRefund  │ applyRefund
            │             │  applyRefund│ (未发货)        │ (售后期)     │ (售后期)
            ▼             │             ▼                 ▼              ▼
       CANCELLED ●        └─────────> REFUNDING <─────────┴──────────────┘
                                       退款中 │
                                              │ refundDone
                                              ▼
                                          REFUNDED ●

   ● = 终态（接受态）    [*] = 初始
```

### 4.6 工程设计要点（从"能画图"到"能扛生产"）

真正的难点不在画图，而在这 7 条：

1. **幂等性（最高频坑）** — 支付回调会重复推送。`PAID` 再收 `pay` 事件，guard 检测"已是 PAID"→ 直接返回成功，**不重复扣库存**。状态机 + 乐观锁天然提供幂等。
2. **并发控制（CAS）** — "用户取消"与"支付到账"可能并发。乐观锁版本号条件更新：
   ```sql
   UPDATE orders SET status='PAID', version=version+1
   WHERE id=? AND status='CREATED' AND version=?   -- 影响行数=0 → 状态已变，拒绝
   ```
   高竞争点也可把订单的所有改动请求**排队串行化**，从设计上消除竞态。
3. **超时驱动转移（时间事件）** — 待付款 30 分钟超时关单、发货后 N 天自动确认。实现：**延迟消息队列**（RocketMQ 延迟级别 / Redis ZSet / 定时扫表）。下单时投递"30 分钟后检查"，到点触发 `payTimeout`。即 Saga 的 timeout 机制（延迟投递 = 把消息发往未来）。
4. **退款是子状态机** — `REFUNDING` 内部还有 `申请→审核→退款中→成功/驳回`，常拆成**独立子状态机**挂在主机上（如 Seata `SubStateMachine` / `CompensateSubMachine`）。
5. **分布式补偿（Saga）** — "支付成功"跨订单/库存/支付多服务，任一失败不整体回滚，而是执行**补偿动作**（退款、恢复库存）达成最终一致。Saga + 状态机是分布式事务标配。
6. **持久化 + 审计** — `status` 存订单表；每次转移写 `order_status_log`(from/to/event/operator/time)，可追溯、对账、排查卡单。注意：**别为每次状态变更重建状态机实例**（代价太高），状态从 DB 拉取。
7. **可观测 / 卡单监控** — 监控停在某态超时的订单（如 `PAID` 超 X 小时未 `ship` → 告警）。

### 4.7 实现选型

| 方案 | 适用场景 |
|---|---|
| `if/else` 分支判断 | 状态极少、临时脚本 |
| **查表法**（转移表 Map + DB `status` + CAS） | 状态多、**最常用最朴素**，推荐起步 |
| Spring StateMachine | Java、配置化、可持久化（状态从 DB 拉取） |
| XState | JS/TS，context 持久化携带订单数据 |
| Temporal / Camunda / Seata Saga | 长流程、跨服务补偿、人工审批节点 |

> **对立观点**：Temporal 主张状态机随状态/转移增多会"越来越难建、难测、难维护"，转用 workflow-as-code 自动接管状态/排队/去重/恢复。结论：**小到中等流程用状态机最清晰；超长、强补偿、含人工节点的流程，工作流引擎可能更省心**。

---

## 5 回连 lesson6：δ 从"硬编码"换成"LLM 推理"

| 维度 | 订单状态机 | lesson6 `run_loop` |
|---|---|---|
| 是不是 FSM | 是（多态） | 是（两态 RUNNING/DONE） |
| 状态存哪 | DB `status` 字段 | `AgentState`（内存对象） |
| 转移**触发源** | **外部事件**（支付回调/点击/定时器） | **LLM 决策**（每步问模型） |
| 转移函数 δ | **硬编码**的转移表 + guard | **LLM 推理**出下一动作 |

**最关键的洞见**：订单状态机和 lesson6 的 agent loop **结构同形**（有限状态 + 受约束转移 + 终态），**唯一根本差别是转移函数 δ**——传统业务里 δ 是程序员写死的规则表；AI agent 里 **δ 被换成了 LLM 的推理**。这就是"agent"区别于"传统状态机程序"的本质：**把确定性的转移规则，替换成由模型动态决定的转移**。代价是失去了确定性与可审计性（订单 δ 可穷举验证，LLM δ 不能）。

---

## 6 信源

**状态机理论**
- [Wikipedia: Finite-state machine](https://en.wikipedia.org/wiki/Finite-state_machine)（含 turnstile 例子、History）
- [Wikipedia: Deterministic finite automaton](https://en.wikipedia.org/wiki/Deterministic_finite_automaton)
- [Stanford CS — Automata Theory Basics](https://cs.stanford.edu/people/eroberts/courses/soco/projects/2004-05/automata-theory/basics.html)
- [Springer — Finite Automata](https://link.springer.com/chapter/10.1007/978-3-540-39886-8_3)
- 教材：Sipser《Introduction to the Theory of Computation》第 1 章；Hopcroft & Ullman《Introduction to Automata Theory…》（无官方免费全文，仅作锚点）
- [RFC 9293 — Transmission Control Protocol](https://www.rfc-editor.org/rfc/rfc9293.html)（TCP 状态机）

**订单状态机 / 工程实践**
- [设计模式：状态机与状态模式（订单流转）](https://blog.csdn.net/qq_38249409/article/details/132229581)
- [人人都是产品经理：订单状态机流转](https://www.woshipm.com/pd/4361843.html)
- [电商订单状态流转技术方案（附源码）](https://cloud.tencent.com/developer/article/1849761)
- [Java 状态机实现订单控制](https://www.cnblogs.com/jajian/p/16784629.html)
- [Idempotency Explained (Leapcell)](https://leapcell.io/blog/idempotency-explained) · [乐观锁解决高并发幂等](https://javamana.com/2021/07/20210720112115937s.html)
- [并发请求竞态控制策略](https://www.zigpoll.com/content/what-are-the-best-strategies-for-managing-concurrent-api-requests-to-prevent-race-conditions-in-our-order-processing-dashboard)
- [SAGA + State Machine 分布式事务](https://medium.com/@dorinbaba/how-we-used-saga-and-state-machine-for-distributed-transactions-2efa8954452e) · [SAGA 设计模式](https://www.geeksforgeeks.org/system-design/saga-design-pattern/) · [NServiceBus Saga Timeouts](https://docs.particular.net/nservicebus/sagas/timeouts) · [Seata Saga 模式](https://seata.apache.org/docs/user/mode/saga/)
- [Spring StateMachine 指南 (Baeldung)](https://www.baeldung.com/spring-state-machine) · [Spring StateMachine 持久化](https://docs.spring.io/spring-statemachine/docs/1.1.x/reference/html/sm-persist.html) · [Temporal: Beyond State Machines](https://temporal.io/blog/temporal-replaces-state-machines-for-distributed-applications)

**Agent 框架**
- [LangGraph overview（官方）](https://docs.langchain.com/oss/python/langgraph/overview) · [LangGraph StateGraph API](https://reference.langchain.com/python/langgraph/graph/state/StateGraph) · [langchain-ai/langgraph (GitHub)](https://github.com/langchain-ai/langgraph)
